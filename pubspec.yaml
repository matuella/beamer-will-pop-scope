name: beamer_will_pop_scope
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
    
  beamer:
    git:
      url: https://github.com/slovnicki/beamer.git
      path: package